from __future__ import annotations
"""
Superseded — configuration now lives in the config/ package (config/__init__.py,
config/markets.py). `import config` resolves to that package, not this file;
this module is never executed. Kept only because deletion is disabled for this repo.
"""
